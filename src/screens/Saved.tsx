import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Saved = () => {
  return (
    <View>
      <Text>Saved houses .tsx</Text>
    </View>
  );
};

export default Saved;

const styles = StyleSheet.create({});
